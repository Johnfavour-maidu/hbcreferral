export function AuthForm({ children }: { children: React.ReactNode }) {
  return (
    <div style={{ width: "100%", maxWidth: 380, marginLeft: "auto", marginRight: "auto" }}>
      {children}
    </div>
  );
}
